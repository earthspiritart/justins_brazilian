<!DOCTYPE html>
<html>
<head>
  <title>JustinsBrazilianCookery</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="one">
    <h3>Justin's Brazilian Cookery</h3>
  </div>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: name %>
    <% end %>
    <% if logged_in? %>
      <span id="ten">Que Pasa? <%= current_user.name%></span>
      <span id="eleven"><%= link_to("logout", logout_path, 
      method: :delete) %></span>
    <% else %>
      <%= link_to("login Please", login_path) %>
      <%= link_to("Signup", new_user_path) %>
    
    <% end %>

<div id="four"><%= yield %><div>
  <div id="two">
      
    <ul id="nav">
      <li><a href="#">About</a></li>
      <li><a href="#">Home</a></li>
      <li><a href="#">Chefs</a></li>
      <li><a href="#">Recipes</a></li>
    </ul>
    <div id="eight"></div>
  </div>
<div id="six">
  <div id="seven"></div>
</div>
</body>
</html>
